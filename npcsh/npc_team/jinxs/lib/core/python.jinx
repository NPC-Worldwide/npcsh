jinx_name: python
description: Execute Python code directly. Code runs immediately — do NOT wrap in function definitions unless you also call them. To create a file, use open('/path', 'w') and write to it. To create a script file, write the script content to the file with open(). Set the "output" variable to a result string.
inputs:
- code
steps:
- code: '{{code}}'
  engine: python
