---
name: yap
description: Voice chat TUI - speech-to-text input, text-to-speech output
---

# yap

Voice chat TUI - speech-to-text input, text-to-speech output

## Inputs

- `model` (default: `None`)
- `provider` (default: `None`)
- `tts_model` (default: `'kokoro'`)
- `voice` (default: `'af_heart'`)
- `files` (default: `None`)
- `show_setup` (default: `False`)

## Steps

- `yap_tui` → [`yap_tui.py`](./yap_tui.py)

## Usage

```
/run_jinx jinx_ref=yap input_values={"model": null, "provider": null, "tts_model": "kokoro", "voice": "af_heart", "files": null, "show_setup": false}
```
