---
name: spool
description: Interactive chat mode - simple conversational interface with an NPC
---

# spool

Interactive chat mode - simple conversational interface with an NPC

## Inputs

- `model` (default: `None`)
- `provider` (default: `None`)
- `attachments` (default: `None`)
- `stream` (default: `True`)

## Steps

- `spool_repl` → [`spool_repl.py`](./spool_repl.py)

## Usage

```
/run_jinx jinx_ref=spool input_values={"model": null, "provider": null, "attachments": null, "stream": true}
```
