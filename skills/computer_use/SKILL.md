---
name: computer_use
description: Vision-based GUI automation TUI - visual task management and action automation
---

# computer_use

Vision-based GUI automation TUI - visual task management and action automation

## Inputs

- `task` (default: `None`)
- `vmodel` (default: `None`)
- `vprovider` (default: `None`)
- `max_iterations` (default: `10`)
- `debug` (default: `False`)

## Steps

- `plonk_tui` → [`plonk_tui.py`](./plonk_tui.py)

## Usage

```
/run_jinx jinx_ref=computer_use input_values={"task": null, "vmodel": null, "vprovider": null, "max_iterations": 10, "debug": false}
```
