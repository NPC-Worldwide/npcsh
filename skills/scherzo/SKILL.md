---
name: scherzo
description: Audio creation studio TUI - generate speech and audio with TTS engine
  controls
---

# scherzo

Audio creation studio TUI - generate speech and audio with TTS engine controls

## Inputs

- `text` (default: `None`)
- `engine` (default: `None`)
- `voice` (default: `None`)
- `output_path` (default: `None`)
- `speed` (default: `None`)

## Steps

- `scherzo_tui` → [`scherzo_tui.py`](./scherzo_tui.py)

## Usage

```
/run_jinx jinx_ref=scherzo input_values={"text": null, "engine": null, "voice": null, "output_path": null, "speed": null}
```
