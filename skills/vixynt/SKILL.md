---
name: vixynt
description: Image creation studio TUI - generate and manage images with parameter
  controls
---

# vixynt

Image creation studio TUI - generate and manage images with parameter controls

## Inputs

- `prompt` (default: `None`)
- `model` (default: `None`)
- `provider` (default: `None`)
- `output_name` (default: `None`)
- `attachments` (default: `None`)
- `n_images` (default: `None`)
- `height` (default: `None`)
- `width` (default: `None`)

## Steps

- `vixynt_tui` → [`vixynt_tui.py`](./vixynt_tui.py)

## Usage

```
/run_jinx jinx_ref=vixynt input_values={"prompt": null, "model": null, "provider": null, "output_name": null, "attachments": null, "n_images": null, "height": null, "width": null}
```
