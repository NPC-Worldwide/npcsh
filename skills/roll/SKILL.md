---
name: roll
description: Video creation studio TUI - generate and manage videos with parameter
  controls
---

# roll

Video creation studio TUI - generate and manage videos with parameter controls

## Inputs

- `prompt` (default: `None`)
- `model` (default: `None`)
- `provider` (default: `None`)
- `output_path` (default: `None`)
- `num_frames` (default: `None`)
- `width` (default: `None`)
- `height` (default: `None`)

## Steps

- `roll_tui` → [`roll_tui.py`](./roll_tui.py)

## Usage

```
/run_jinx jinx_ref=roll input_values={"prompt": null, "model": null, "provider": null, "output_path": null, "num_frames": null, "width": null, "height": null}
```
